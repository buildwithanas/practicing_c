#include <stdio.h>

int main() {
  //BOOLEAN

  bool isonline = true;
  bool isoffline = false;
  bool isaboy = 0;

  printf("Is the user online? %d\n", isonline);
  printf("Is the user offline? %d\n", isoffline);
  printf("He is a boy? %d\n", isaboy);

  return 0;
}